const { supabaseClient } = require('./bot-assistant/dataService.js');

async function test() {
    console.log('Testing...');
    // find a cancelled inventory document or something to see error
    // actually just mock the call to cancelOrder or insert movements
}
test();
