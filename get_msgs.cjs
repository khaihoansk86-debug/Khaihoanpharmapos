const fs = require('fs'); const lines = fs.readFileSync('C:/Users/Admin/.gemini/antigravity-ide/brain/e410a359-dc12-4848-972a-e9d4f5c9150a/.system_generated/logs/transcript.jsonl', 'utf8').split('\n'); for(let line of lines) { if (line.includes('USER_INPUT')) { try { const d = JSON.parse(line); console.log(d.content.split('\n').filter(l => !l.includes('ADDITIONAL')).join(' ').substring(0,200)); } catch(e){} } }
